export default function AdminPage() {
  return (
    <section>
      <h2 className="text-2xl font-bold mb-4">Admin Dashboard</h2>
      <p>Admin area (protected). Hook this up to Supabase auth and check for admin role before rendering.</p>
    </section>
  )
}
